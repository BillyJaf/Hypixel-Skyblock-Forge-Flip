import type { Filters } from "../context/FilterContext";
import type { ForgeItem } from "../types/forge";

const applyFilters = (forgeItems: ForgeItem[], filters: Filters) => {
    return forgeItems
}